doctype html
html(lang="pl")
  head
    meta(charset="UTF-8")
    meta(name="viewport" content="width=device-width, initial-scale=1.0")
    title Dodaj Notatkę
    link(href="https://fonts.googleapis.com/css2?family=Helvetica:wght@300;400;500&display=swap" rel="stylesheet")
    style.
      body {
        font-family: 'Helvetica', sans-serif;
        background-color: #f0f0f0;
        color: #333;
        margin: 0;
        padding: 0;
        display: flex;
        flex-direction: column;
        min-height: 100vh;
      }
      header {
        display: flex;
        justify-content: space-between;
        align-items: center;
        background-color: #0071e3;
        padding: 20px;
        color: white;
      }
      header h1 {
        margin: 0;
        font-size: 1.5rem;
      }
      .container {
        flex: 1;
        padding: 20px;
        max-width: 600px;
        margin: 0 auto;
      }
      form {
        background-color: #fff;
        padding: 20px;
        border-radius: 8px;
        box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        display: flex;
        flex-direction: column;
        gap: 15px;
      }
      label {
        font-size: 1.1rem;
        color: #333;
      }
      input[type="text"],
      textarea {
        width: 100%;
        padding: 10px;
        border: 1px solid #ddd;
        border-radius: 5px;
        font-size: 1rem;
        resize: vertical;
      }
      textarea {
        min-height: 150px;
      }
      .submit-button {
        background-color: #0071e3;
        color: white;
        padding: 10px 20px; /* Zwiększenie paddingu */
        border: none;
        border-radius: 5px;
        cursor: pointer;
        font-size: 1rem; /* Oryginalny rozmiar czcionki */
        transition: background-color 0.3s;
      }
      .submit-button:hover {
        background-color: #005bb5;
      }
      .dashboard-button {
        background-color: #fff;
        color: #0071e3;
        padding: 10px 20px; /* Oryginalny padding */
        border: none;
        border-radius: 5px;
        cursor: pointer;
        font-size: 1rem; /* Oryginalny rozmiar czcionki */
        transition: background-color 0.3s;
      }
      .dashboard-button:hover {
        background-color: #f0f0f0;
      }
      footer {
        background-color: #0071e3;
        color: white;
        text-align: center;
        padding: 15px;
        font-size: 0.9rem;
      }

      footer p {
        margin: 0;
      }

  body
    // Header
    header
      h1 Dodaj Nową Notatkę
      form(action="/dashboard" method="GET")
        button.dashboard-button Wróć do dashboardu

    // Content
    .container
      form(action="handling/createNote" method="POST")
        label(for="title") Tytuł Notatki:
        input(type="text" id="title" name="title" placeholder="Wprowadź tytuł notatki" required)

        label(for="body") Treść Notatki:
        textarea(id="body" name="body" placeholder="Wprowadź treść notatki" required)

        button.submit-button(type="submit") Dodaj Notatkę

    // Footer
    footer
      p &copy; 2024 Aplikacja do Notatek. Wszystkie prawa zastrzeżone. Koty są super!
