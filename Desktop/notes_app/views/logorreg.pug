doctype html
html(lang="pl")
  head
    meta(charset="UTF-8")
    meta(name="viewport" content="width=device-width, initial-scale=1.0")
    title Wybór Rejestracji lub Logowania
    link(href="https://fonts.googleapis.com/css2?family=Helvetica:wght@300;400;500&display=swap" rel="stylesheet")
    style.
      * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
      }

      body {
        font-family: 'Helvetica', sans-serif;
        background-color: #f5f5f7;
        color: #333;
        line-height: 1.6;
      }

      h1, h2 {
        font-weight: 500;
        color: #333;
      }

      a {
        text-decoration: none;
        color: inherit;
      }

      .container {
        max-width: 1200px;
        margin: 0 auto;
        padding: 60px 20px;
        text-align: center;
      }

      h1 {
        font-size: 3.5rem;
        margin-bottom: 30px;
        letter-spacing: -0.02em;
      }

      .content {
        background-color: #ffffff;
        padding: 50px 30px;
        border-radius: 12px;
        box-shadow: 0 10px 30px rgba(0, 0, 0, 0.08);
        max-width: 800px;
        margin: 0 auto;
        text-align: center;
      }

      .content p {
        font-size: 1.1rem;
        color: #555;
        margin-bottom: 25px;
        letter-spacing: 0.01em;
      }

      .btn {
        display: inline-block;
        background-color: #0071e3;
        color: white;
        padding: 15px 40px;
        font-size: 1.2rem;
        font-weight: 600;
        border-radius: 30px;
        transition: background-color 0.3s ease, transform 0.2s ease;
        letter-spacing: 0.05em;
        text-transform: uppercase;
        margin: 10px;
      }

      .btn:hover {
        background-color: #0060b3;
        transform: translateY(-2px);
      }

      .btn:active {
        background-color: #0055a5;
        transform: translateY(0);
      }

      .info {
        margin-top: 40px;
        font-size: 1rem;
        color: #777;
      }

      .info a {
        color: #0071e3;
        font-weight: bold;
      }

      footer {
        margin-top: 60px;
        text-align: center;
        color: #888;
        font-size: 0.9rem;
      }

      footer p {
        margin: 0;
      }

      @media (max-width: 768px) {
        .container {
          padding: 40px 20px;
        }

        h1 {
          font-size: 2.8rem;
        }

        .content {
          padding: 40px 20px;
        }

        .btn {
          padding: 12px 30px;
        }
      }
  body
    .container
      h1 Witaj w naszej aplikacji!
      .content
        p
          | Zaloguj się lub zarejestruj, aby rozpocząć korzystanie z aplikacji do zarządzania notatkami. 
          | Wszystkie Twoje dane są bezpieczne, a nasza aplikacja jest zabezpieczona za pomocą nowoczesnego szyfrowania.
        
        a.btn(href="/register") Zarejestruj się
        a.btn(href="/login") Zaloguj się

    footer
      p &copy; 2024 Aplikacja Notes-App. Wszystkie prawa zastrzeżone.
