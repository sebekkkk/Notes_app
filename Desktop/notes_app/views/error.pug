doctype html
html(lang="pl")
  head
    meta(charset="UTF-8")
    meta(name="viewport" content="width=device-width, initial-scale=1.0")
    title Błąd
    link(href="https://fonts.googleapis.com/css2?family=Helvetica:wght@300;400;500&display=swap" rel="stylesheet")
    style.
      * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
      }

      body {
        font-family: 'Helvetica', sans-serif;
        background-color: #f5f5f7;
        color: #333;
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
        height: 100vh;
        text-align: center;
        padding: 20px;
      }

      h1 {
        font-size: 4rem;
        font-weight: 500;
        color: #d9534f;
        margin-bottom: 20px;
      }

      p {
        font-size: 1.2rem;
        color: #555;
        margin-bottom: 30px;
      }

      .btn {
        display: inline-block;
        background-color: #0071e3;
        color: white;
        padding: 15px 40px;
        font-size: 1.2rem;
        font-weight: 600;
        border-radius: 30px;
        transition: background-color 0.3s ease, transform 0.2s ease;
        text-transform: uppercase;
      }

      .btn:hover {
        background-color: #0060b3;
        transform: translateY(-2px);
      }

      .btn:active {
        background-color: #0055a5;
        transform: translateY(0);
      }

      .error-img {
        max-width: 300px;
        height: auto;
        margin-bottom: 30px;
      }

  body
    h1 Błąd
    .error-img
      iframe(src="https://tenor.com/embed/16335417" width="100%" height="auto" frameborder="0" scrolling="no")
    p Przepraszamy, coś poszło nie tak
    a.btn(href="/") Powrót do strony głównej
